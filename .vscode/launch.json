{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/.build/debug/Run",
            "args": [
                "serve"
            ],
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "build"
        }
    ]
}
