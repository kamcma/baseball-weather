{
    "buildpacks": [
        {
            "url": "https://github.com/vapor-community/heroku-buildpack"
        }
    ],
    "environments": {
        "test": {
            "scripts": {
                "test-setup": "sudo apt-get update && sudo apt-get install swift",
                "test": "swift test && exit $?"
            }
        }
    }
}
